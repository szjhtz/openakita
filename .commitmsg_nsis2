fix(nsis): 升级时旧卸载器始终以静默模式运行，避免弹出二次确认窗口

旧版卸载器以交互模式启动时会弹出自己的确认窗口，该窗口可能被其他
窗口遮挡导致用户无法操作，最终卸载器返回非零退出码触发"无法卸载"。
现在 reinst_uninstall 阶段始终追加 /P /UPDATE 参数：
- /P 跳过确认页面并自动关闭（用户已在新安装器的重装页面确认过）
- /UPDATE 保留快捷方式和用户数据，符合升级语义
